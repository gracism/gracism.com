{{> head}}

<section class="thought-section">
    <div class="thought-label">
        <a href="thoughts.html">Thought for the day</a>
    </div>
    <div class="thought-text">
        {{thought}}
    </div>
</section>

<section class="content-grid">
    <article class="content-card">
        <h2>{{hleft}}</h2>
        <p>{{tleft}}</p>
    </article>
    
    <article class="content-card">
        <h2>{{hright}}</h2>
        <p>{{tright}}</p>
    </article>
</section>

{{> foot}}
